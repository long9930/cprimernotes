#include <stdio.h>
#include <lexer.h>
#include <counter.h>


int main( int argc, char ** argv )
{
    //yylex();
    int newCount[4];
    counter(newCount);
    printf("%d %d %d %d \n", newCount[0], newCount[1],
	   newCount[2], newCount[3]);

    for
    
    //printf( "%d %d %d %d \n", fee_count, fie_count,
    //	    foe_count, fum_count );
    return 0;
}




    
